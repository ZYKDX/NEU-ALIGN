“”“
normal binary search problem, familiarize!

time complexity not required
”“”

class Solution(object):
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        low = 0
        high = len(nums) - 1
        guess = (low + high) // 2
        while low <= high:
            if nums[guess] == target:
                return guess
            elif nums[guess] < target:
                low = guess + 1
                guess = (low + high) // 2
            else:
                high = guess - 1
                guess = (low + high) // 2
        return -1
